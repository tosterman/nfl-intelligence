# Native Git publication verification — 2026-09-10

[Workflow 34504966362](https://github.com/tosterman/nfl-intelligence/actions/runs/34504966362) completed successfully. It refreshed model source acquisition and weather, passed 67 Python and 25 TypeScript tests and the build, pushed edition commit `8548dcf43d4063990c40835761ed5fb091875cde`, and verified the exact public forecast artifact after the authenticated Vercel status succeeded. Capture at `2026-09-10T16:56:04.338272+00:00` retained 105 canonical snapshot hashes. Receipt archive commit: `cc223cd2852e2ef4ade18477c5e77739da46205e`. The recovery artifact `publication-recovery-34504966362-1` exists, is unexpired, and is 136217 bytes.

An independent authenticated Vercel API check also confirmed `dpl_CnWK6TT1ncwKzpgzMbqmA5AkwiYf` READY, source git, URL `https://nfl-intelligence-qilwsvo1t-khnum.vercel.app`. The first canary implementation inferred this API-style ID from the status URL; that exact ID was subsequently confirmed by this separate API lookup. The reviewed implementation now stores only the observed `providerDeploymentRef` for native receipts. The original canary receipt remains preserved rather than rewritten.

The job uses its scoped GitHub token (contents write, statuses read), with no personal Vercel token. The first push refuses concurrent branch advancement rather than rebasing an already-tested release. It reads the actual pushed HEAD, freezes the intended artifact/ledger and source commit in recovery evidence, waits on the latest exact-commit Vercel status from observed bot identity 35613825, and compares all public artifact contents and canonical snapshot hashes. Pending statuses, failed providers, stale alias contents and malformed/login responses cannot create a receipt. Capture time, not provider status time, determines pre-kickoff eligibility.

The public production alias is mutable. Its matching response proves public availability of those forecast bytes at capture time; it does not by itself identify the deployment serving the alias. Native Git status evidence and public capture evidence are explicitly distinguished. Receipt-only pushes can create another deployment, but do not recursively generate more receipts. The receipt push uses the bounded archive retry helper, with immutable receipt sourceCommit retained and recovery bundles available if a conflict fails.

Independent review identified and resolved first-push rebase risk and inferred identifier labeling. Four native-publication tests cover authenticated identity/project/time validation, latest-state handling, exact artifact binding, delayed alias propagation and status lookup failure. Existing direct-API receipt tests also pass after sharing the artifact verifier. A manually dispatched complete run proves the mechanics; it is not a claim of long-term scheduler availability, recovery-drill completion or tamper-proof notarization. The existing daily schedule remains 10:30 UTC.
