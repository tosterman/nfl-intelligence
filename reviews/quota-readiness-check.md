# Observed quota and hosting access

The local production environment export declares the odds key but its parsed value is empty. A direct Node parser confirmed this is not an inherited-variable override. No provider request was made by the attempted local quota checks.

The connected Vercel tool returned the expected Khnum team but an empty project list; fetching the configured project returned 404. This is an access/configuration discrepancy, not proof that the project was deleted. The public status endpoint returned HTTP 200 at September 11 00:50 UTC with healthy sources and the older September 10 20:09 edition. The raw status receipt is `hosting-access-check.json`. No deployment retry was attempted.

GitHub's latest completed odds run, 34541012669, provides stronger quota evidence. Its captured result reported 470 credits remaining at September 10 23:11:44.852 UTC. No downloadable collection artifact was listed for that older workflow run, so the value was verified in the authenticated run log; `observed-odds-quota.json` retains the selected result identity and log-line hash, without credentials or quote payloads.

At that observation only, the 153-request candidate's nominal cost of 459 credits would leave 11 credits. This does not establish current quota, a reset date, complete failed-attempt history or sufficient future capacity. Existing collectors can continue consuming credits. No startup history was fabricated or reservation state written to make the candidate fit.

Before any adaptive collector is enabled, obtain a fresh server-side quota observation and reconcile prior usage explicitly. If conservative startup allowances are needed, represent them as allowances rather than fabricated historical request timestamps. The current scheduler remains unchanged.
