# Listed quarterback context — September 10, 2026

The matchup personnel panel now shows who is listed first at quarterback for each team in a recent nflverse depth-chart snapshot. This is explicitly not a confirmed starter. Provider record timestamps are not announcement timestamps, and this context does not change model predictions.

The collector verifies current-season asset URL, byte length, SHA-256 release digest, and source freshness. It reuses the previously tested prior-only depth-chart selection, including latest-team-snapshot selection across all positions, conflict rejection and a thirty-hour limit. The real acquisition returned eligible roles for all 32 teams. The panel checks season, team, source acquisition time, asset time, role time and kickoff; roles disappear after thirty hours or kickoff, including while the page remains open. Failed collection retains the last verified snapshot with its original times and displays a failure notice.

The daily refresh workflow includes an independent collection step and publishes role snapshots, collection state and archives. This step uses no private credentials or model inputs. The dedicated production health monitor has not yet been extended to quarterback roles; collection failure is currently visible in the saved state, workflow step and panel.

Review caught an archive-growth problem before release: saving a fresh full annual CSV on every run would add about 9.6 MB per changed snapshot at today's size. The final implementation stores exact CSV bytes in content-addressed contiguous timestamp blocks and an ordered full-source manifest. Today's 48.5 MB source reconstructs from 175 blocks. Unchanged blocks are reused, preserving source-level SHA-256 verification. Corrections to historical blocks still increase storage; bounded future growth is not guaranteed. The first full-gzip draft was moved to ignored local recovery storage and is not published in Git.

Validation: 48 application tests, four collection/archive tests and five prior-role tests pass. The real source was reconstructed byte-for-byte and normalized roles reproduced for all teams. A CRLF-preserving prepend test verifies deduplication. Independent review verified the archive correction and found no gating defect. Local browser review confirmed the Tampa Bay/Cincinnati roles, timestamps, disclaimers and visual layout. No new physical-device mobile check or confirmed lineup verification is claimed.
