# Matchup outcome ranges

The model outlook now expresses signed home-margin boundaries using the relevant team name and displays the retained total-points interval. This exposes existing model output without changing predictions. The notice distinguishes separate marginal coverage from joint coverage, outcome variability from confidence in a mean, and continuous boundaries from actual whole-number football scores.

Two targeted tests passed for side/sign mapping, zero, one-sided ranges and malformed intervals. Type checking passed. Chromium and WebKit at 320px displayed both ranges without document overflow; captured text is in `outcome-range-browser.json`. Independent statistical/reader source review found no actionable issue. This change is local and has not been deployed.
