# Surface the unresolved report identity

The known `identifier-mismatch` finding now appears beside its personnel report, outside the collapsed historical-participation section. It states that the reported ID disagrees with the same-name depth-chart entry and leaves the identity unresolved. It does not select a replacement ID, infer health, or assert that a matched ID confirms availability.

The warning shares the usage selector's exact source/retrieval/player-scope/name binding and time validation. An old warning cannot be applied to a changed report. Other unavailable histories do not receive an identifier-conflict claim without that recorded finding. Historical numeric usage remains withheld for the conflict.

All 97 application tests and TypeScript checking pass. Chromium and WebKit at 320 pixels verified the actual James Thompson Jr. report shows the warning outside details, with no document overflow. Results are in `personnel-identity-warning.json`. This is a source-disagreement disclosure, not resolution of the underlying data conflict. Production rollout remains pending.
