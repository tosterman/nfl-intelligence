# Personnel snapshot pipeline — September 10, 2026

`python scripts/refresh_personnel.py` now acquires the current season's nflverse injury/practice CSV and verifies its SHA-256 and byte count against GitHub release metadata. It rejects unknown status values, duplicate player/week identities, missing player IDs, wrong seasons, unsupported game types, invalid weeks and unexpected teams. Blank game statuses remain null. Practice and game status are separate fields.

The first real capture contains 139 rows. `data/personnel.json` carries source identity, release update time, acquisition time, attribution and transformation notes. `data/personnel-sources/` retains the exact CSV compressed with deterministic gzip and a separately hashed normalized capture. Reproduction tests match normalized rows to the original bytes and validate archived capture hashes. Data is attributed to [nflverse](https://github.com/nflverse/nflverse-data/releases/tag/injuries), with the repository's [CC BY 4.0 license](https://github.com/nflverse/nflverse-data/blob/main/LICENSE.md). Our normalization and lack of individual report times are explicitly recorded.

The game-selection function requires exact season/type/week/team matching. Both acquisition and asset update must be within thirty hours and cannot be future-dated. Re-downloading an old asset does not reset its age. Current selection closes at kickoff and excludes captures acquired at/after kickoff. A missing team is returned as unknown coverage, never as a healthy roster. This function selects recent observations; it cannot prove the actual date of each practice report.

Focused tests cover scope/status rejection, empty game statuses, missing team coverage, duplicate identities, old-asset redownloads, postgame/future exclusions and real archived-byte reproduction. No numerical model changes were made. The pipeline is now connected to the daily forecast workflow; the public matchup page does not yet consume it. Acquisition failures record a separate unavailable state and leave the previous pointer intact, whose timestamps must be checked by consumers.

Independent code review found a postseason enum mismatch: schedules use WC/DIV/CON/SB while reports use POST. The selector now normalizes these phases and regression coverage tests all four, while excluding REG matches. Seven focused tests pass after this correction; the broader 81-test suite passed before this localized correction.

## Scheduled collection and failure isolation

The daily 10:30 UTC workflow collects personnel independently with `continue-on-error`. Both publication staging paths and the ninety-day recovery artifact include the current personnel snapshot, its collection status and compressed source/capture archives. A successful overall forecast workflow does not imply successful personnel acquisition: `data/personnel-collection.json` is the independent acquisition result. The forecast publication receipt validates the forecast artifact, not personnel content.

Snapshot replacement uses a same-directory temporary file and atomic replacement. Nine focused tests pass, including a forced replacement failure after successful source validation: previous bytes remain intact, temporary files are cleaned up and the failed collection is recorded. A source failure test separately preserves the previous pointer. Independent review found no release blocker in this integration. The public panel and ongoing personnel monitoring remain required follow-up work.

The [real manually dispatched workflow 34510282632](https://github.com/tosterman/nfl-intelligence/actions/runs/34510282632) succeeded through collection, 83 Python tests, application tests/build, native deployment, forecast capture and archive push. Personnel collection independently reported `ok`, capturing 139 rows at `2026-09-10T17:47:09.772903+00:00`. Recovery artifact `10165536436` was downloaded and inspected: it contains the collection result, current snapshot and three capture archives; the recovered CSV reproduced the expected source hash. Receipt commit: `3901e4b81e04d49c2898f4508f7d01cd8d55be1a`. This validates the real workflow path, not sustained clock-triggered reliability. The additional atomic-failure test passes locally after this run.
