# Production freshness monitoring — 2026-09-10

`Check production data health` runs hourly at minute 47 UTC and can be manually dispatched. It probes the public forecast and odds health endpoints without sending analytics events, fetching sportsbook data, changing forecasts or requiring secrets. Each probe has a ten-second HTTP timeout and one retry after two seconds; redirects fail rather than being followed to a login or unrelated page. The two probes run concurrently.

The checker requires HTTP 200 plus the expected health payload. It independently recomputes acquisition ages from timezone-aware timestamps. Forecast checks require the model edition, schedule/results and the exact efficiency pair for the endpoint's explicit season (season minus one and season). The season must be the current or preceding calendar year. All timestamps must remain inside the thirty-hour window, with the existing five-minute clock tolerance. Odds must have a valid acquisition no older than six hours and no future timestamp. Rounded displayed ages and a claimed status alone cannot make stale data pass. Missing identities, malformed payloads and bad source fingerprints fail closed.

A failed endpoint makes the process/job fail. A sanitized evidence file retains each attempt, its observation time, status code and bounded reason. The workflow uploads that file even after failure and retains it fourteen days. GitHub Actions provides failure visibility under the owner's notification settings; delivery of email/push notifications has not been independently verified. This is monitoring hosted on the same scheduler as collection, not an independent external uptime service. GitHub outages, delayed/dropped schedules or disabled workflows can affect both. No uptime SLA or full sportsbook coverage is claimed.

Local tests reject stale timestamps behind claimed green responses, incomplete checks, future/old odds, implausible efficiency years and bad HTTP statuses. A real local HTTP failure drill served 503 for forecasts and a valid odds response: the checker retried, returned exit code 1, and retained both failed attempts alongside the healthy odds check. The live read-only probe passed both production endpoints on September 10. The clock-triggered workflow still requires sustained observation.

## Deployed verification

Commit `6bb0bc47996271b9d8823aa3c34a5f41f7f5eaf6` received a successful native Vercel deployment status and passed [product/model CI](https://github.com/tosterman/nfl-intelligence/actions/runs/34509152650). The final checker, including exact season-pair validation, passed production locally at 17:36:15 UTC. [Manually dispatched health run 34509230520](https://github.com/tosterman/nfl-intelligence/actions/runs/34509230520) then checked production at `2026-09-10T17:36:28.108536+00:00`: both endpoints returned HTTP 200 and passed timestamp validation. Evidence artifact `10165079331`, `production-health-34509230520-1`, was retained (385 compressed bytes). This verifies deployment and the real workflow path; it does not establish sustained hourly operation or notification delivery.

Adversarial review reproduced a false-green response using implausible future efficiency-source years. The endpoint now exposes its season, and the checker requires the exact preceding/current season pair with a plausible season bound. The regression test passes alongside the other three health tests.

## Personnel monitoring extension

The hourly workflow now probes `/api/personnel-status` alongside forecasts and odds. It requires a successful independent collection result, the matching site season, a nonempty file with a source hash, and actual acquisition, asset-update and collection-check timestamps inside thirty hours. An old asset cannot be made healthy by re-download. The endpoint returns 503 on failure and exposes only aggregate identity/freshness metadata, not individual player reports. It explicitly does not certify per-report dates or complete team coverage.

Five Python monitor tests pass, including the real HTTP failure drill with healthy odds and personnel recorded separately. All 42 application tests and the production build pass. Personnel-specific assertions reject failed collection, stale source timestamps, season mismatch and malformed source identity. These supplement the source-acquisition and panel tests.

Production deployment of `6ed85f6` succeeded. The [three-source workflow run 34511135272](https://github.com/tosterman/nfl-intelligence/actions/runs/34511135272) passed at `2026-09-10T17:55:17.928827+00:00`, with HTTP 200 and validated timestamps for forecasts, odds and personnel. Evidence artifact `10165814279` was retained (401 compressed bytes). This is one verified dispatched run, not proof of continuous hourly operation or notification delivery.

## Weather monitoring extension

The same hourly checker now includes `/api/weather-status`. The endpoint derives expected weather coverage from unplayed games with verified coordinates inside the collector's seven-day window. Each expected game must have a matching venue/kickoff, a kickoff-hour forecast, fresh issue/acquisition times, and a source fingerprint. Missing or failed eligible forecasts return 503. Zero eligible games may be healthy, but the collection must still be fresh. This explicitly does not certify all venues, individual weather accuracy, or numerical model effects.

The collector now persists its exact `collectionStartedAt`, separate from completion `generatedAt`. Review caught that using completion time could incorrectly include a game entering the window during collection; a regression test now covers this case. Legacy artifacts lacking the start timestamp fail closed. The monitor independently verifies collection timestamp ordering and ages, per-game source ages, unique game IDs, and coverage counts. It does not acquire forecasts itself.

Local verification: 46 application tests, six Python production-monitor tests, seven weather-acquisition tests, and the production build pass. Real acquisition returned four forecasts and retained hashed source bytes. Type checking initially found weak venue typing and an inferred optional test key; both were corrected before the successful build. Public deployment verification is recorded separately below.
